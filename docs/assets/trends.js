"use strict";
const DATA_URL="data/trends/rolling-30d.json";
const NS="http://www.w3.org/2000/svg";
const q=s=>document.querySelector(s);
function e(tag,cls,text){const n=document.createElement(tag);if(cls)n.className=cls;if(text!==undefined)n.textContent=String(text);return n}
function svg(tag,attrs={}){const n=document.createElementNS(NS,tag);Object.entries(attrs).forEach(([k,v])=>n.setAttribute(k,v));return n}
function modeBadge(mode){const b=e("span",`source-mode-badge ${mode==="native_daily"?"native":""}`,mode==="native_daily"?"Native Daily":"Market Reconstruction");return b}
function themeLabel(data,id){return data.themes?.[id]?.label||id||"—"}
function categoryLabel(data,id){return data.category_labels?.[id]||id}
function renderHero(data){const meta=q("#lens-hero-meta");[["Window",`${data.window_start} → ${data.window_end}`],["Sessions",data.coverage.market_sessions],["Native Daily",data.coverage.native_daily_days],["Reconstructed",data.coverage.reconstructed_days]].forEach(([l,v])=>{const x=e("div");x.append(e("span","",l),e("strong","",v));meta.append(x)});const current=data.days.at(-1);const top=data.persistent_themes?.[0];const transition=data.regime_transitions?.at(-1);const strip=q("#lens-summary-strip");[["Current Regime",current?.regime_label],["Dominant Theme",themeLabel(data,current?.dominant_theme_id)],["Most Persistent",top?.theme_label],["Last Regime Shift",transition?`${transition.date} · ${transition.from} → ${transition.to}`:"No change in window"],["Historical Series",data.coverage.historical_series_start]].forEach(([l,v])=>{const x=e("div");x.append(e("span","",l),e("strong","",v||"—"));strip.append(x)});q("#lens-footer").textContent=`As of ${data.as_of}`}
function renderDayDetail(data,day,target){target.replaceChildren();const title=e("div","lens-detail__title",`${day.date} · ${day.regime_label} · ${themeLabel(data,day.dominant_theme_id)}`);const meta=e("div","lens-detail__meta");meta.append(modeBadge(day.source_mode),document.createTextNode(` · ${day.catalysts.length} catalyst driver(s)`));const ol=e("ol");day.catalysts.forEach(c=>{const li=e("li");li.append(e("strong","",`${c.rank}. ${c.title}`),document.createTextNode(` — ${c.evidence}`));ol.append(li)});target.append(title,meta,ol)}
function renderRibbon(data){const root=q("#lens-full-ribbon");root.style.setProperty("--cols",data.days.length);data.days.forEach(day=>{const b=e("button",`${day.regime_code} ${day.source_mode==="native_daily"?"native":""}`,day.date.slice(5).replace("-","/"));b.type="button";b.title=`${day.date} · ${day.regime_label}`;b.onclick=()=>renderDayDetail(data,day,q("#regime-detail"));root.append(b)});renderDayDetail(data,data.days.at(-1),q("#regime-detail"))}
function renderSignals(data){const root=q("#signal-matrix");root.style.setProperty("--cols",data.days.length);root.append(e("div","signal-matrix__rowlabel","Signal / Date"));data.days.forEach(d=>root.append(e("div","",d.date.slice(5))));const rows=[['growth','Growth'],['inflation','Inflation'],['rates','Rates'],['earnings','Earnings'],['liquidity','Liquidity'],['geopolitics','Geopolitics']];rows.forEach(([key,label])=>{root.append(e("div","signal-matrix__rowlabel",label));data.days.forEach(d=>{const s=d.signals?.[key]||'→';const cls=s==='↑'?'signal-cell--up':s==='↓'?'signal-cell--down':'signal-cell--flat';root.append(e("div",cls,s))})})}
function biasColor(bias){return bias==='risk_off'?'#8b1e2d':bias==='risk_on'?'#245f43':'#817c73'}
function renderCatalystDetail(data,c,day){const target=q("#catalyst-detail");target.replaceChildren();const t=e("div","lens-detail__title",`${day.date} · Rank ${c.rank} · ${c.title}`);const m=e("div","lens-detail__meta");m.append(modeBadge(c.source_mode),document.createTextNode(` · ${themeLabel(data,c.theme_id)} · ${c.importance||'★★★'}`));const p=e("p","",c.evidence);const p2=e("p","",`Transmission: ${c.transmission||'—'}`);target.append(t,m,p,p2)}
function renderCatalystMap(data){const categories=Object.keys(data.category_labels);const dates=data.days.map(d=>d.date);const el=q("#catalyst-map");const W=1180,H=420,left=190,right=22,top=24,bottom=48;el.setAttribute('viewBox',`0 0 ${W} ${H}`);const x=i=>left+(W-left-right)*(dates.length===1?0:i/(dates.length-1));const y=i=>top+(H-top-bottom)*(i/(categories.length-1));categories.forEach((cat,i)=>{const yy=y(i);el.append(svg('line',{x1:left,y1:yy,x2:W-right,y2:yy,stroke:'#d4cec3','stroke-width':1}));const txt=svg('text',{x:4,y:yy+4,class:'catalyst-axis-label'});txt.textContent=categoryLabel(data,cat);el.append(txt)});dates.forEach((d,i)=>{if(i%3!==0&&i!==dates.length-1)return;const txt=svg('text',{x:x(i),y:H-15,class:'catalyst-date-label','text-anchor':'middle'});txt.textContent=d.slice(5);el.append(txt)});data.days.forEach((day,di)=>day.catalysts.forEach(c=>{const ci=categories.indexOf(c.category);if(ci<0)return;const circle=svg('circle',{cx:x(di),cy:y(ci),r:5+(c.importance_level-3)*3,fill:biasColor(c.market_bias),class:`catalyst-dot ${c.source_mode==='native_daily'?'native':''}`,tabindex:0});circle.addEventListener('click',()=>renderCatalystDetail(data,c,day));circle.addEventListener('focus',()=>renderCatalystDetail(data,c,day));const title=svg('title');title.textContent=`${day.date} · #${c.rank} ${c.title}`;circle.append(title);el.append(circle);const rank=svg('text',{x:x(di),y:y(ci)+3,'text-anchor':'middle','font-size':8,fill:'#fff','pointer-events':'none'});rank.textContent=c.rank;el.append(rank)}));const last=data.days.at(-1);if(last?.catalysts?.[0])renderCatalystDetail(data,last.catalysts[0],last);renderMobileCatalysts(data)}
function renderMobileCatalysts(data){const root=q("#mobile-catalyst-weeks");const groups=[];for(let i=0;i<data.days.length;i+=5)groups.push(data.days.slice(i,i+5));groups.forEach((g,idx)=>{const w=e("section","mobile-week");w.append(e("h3","",`Week ${String(idx+1).padStart(2,'0')} · ${g[0].date.slice(5)} → ${g.at(-1).date.slice(5)}`));g.forEach(day=>day.catalysts.forEach(c=>{const card=e("div","mobile-catalyst");card.append(e("strong","",`${day.date.slice(5)} · #${c.rank} ${c.title}`),e("p","",`${themeLabel(data,c.theme_id)} · ${c.evidence}`));card.onclick=()=>renderCatalystDetail(data,c,day);w.append(card)}));root.append(w)})}
function renderThemes(data){const t=q("#theme-table");const head=e("thead"),hr=e("tr");['Theme','State','Days in Top 3','Best Rank','First Seen','Last Seen','Source'].forEach(x=>hr.append(e("th","",x)));head.append(hr);const body=e("tbody");data.persistent_themes.forEach(x=>{const r=e("tr");const cells=[x.theme_label,x.state,x.days_in_top3,`#${x.best_rank}`,x.first_seen,x.last_seen,x.source_modes.map(m=>m==='native_daily'?'Native':'Reconstructed').join(' + ')];cells.forEach((v,i)=>r.append(e("td",i===1?'theme-state':'',v)));body.append(r)});t.append(head,body)}
function sparkPath(points,w,h,pad=6){const vals=points.map(p=>p.value);const min=Math.min(...vals),max=Math.max(...vals),range=max-min||1;return points.map((p,i)=>`${i?'L':'M'} ${pad+(w-pad*2)*(i/(points.length-1||1))} ${pad+(h-pad*2)*(1-(p.value-min)/range)}`).join(' ')}
function renderSparks(data){const root=q("#spark-grid");Object.entries(data.series).forEach(([sid,s])=>{if(!s.points?.length)return;const card=e("article","spark-card");const head=e("div","spark-card__head");head.append(e("h3","",s.label),e("strong","",`${s.points.at(-1).value.toLocaleString(undefined,{maximumFractionDigits:2})}${s.unit==='%'?'%':''}`));card.append(head);const S=svg('svg',{viewBox:'0 0 500 90','aria-label':`${s.label} 30-day trend`});S.append(svg('line',{x1:6,y1:82,x2:494,y2:82,stroke:'#d4cec3','stroke-width':1}));S.append(svg('path',{d:sparkPath(s.points,500,90),fill:'none',stroke:'#171614','stroke-width':2,'vector-effect':'non-scaling-stroke'}));card.append(S);const meta=e("div","spark-card__meta");meta.append(e("span","",s.points[0].date),e("span","",s.source),e("span","",s.points.at(-1).date));card.append(meta);root.append(card)})}
async function init(){try{const r=await fetch(DATA_URL,{cache:'no-store'});if(!r.ok)throw new Error(`${r.status}`);const data=await r.json();renderHero(data);renderRibbon(data);renderSignals(data);renderCatalystMap(data);renderThemes(data);renderSparks(data)}catch(err){console.error(err);q('#lens-app').append(e('div','status-panel status-panel--error',`30D Lens data unavailable: ${err.message}`))}}
document.addEventListener('DOMContentLoaded',init);
